<%= form_for([@gossip,@gossip.comments.build]) do |f| %>
<p>
		<%= f.label :first_name %>
	</p>
	<p>
		<%= f.label :content %><br>
		<%= f.text_area :content %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>
